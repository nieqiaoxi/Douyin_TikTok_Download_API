# 命令

```py

		curl url

    mkdir创建文件
    npm prune  *
    npm init -y


    npm uninstall
    npm list -g --depth 0
    npm uninstall -g name
    create-react-app name --template typescript
    tyarn install

    git
    git branch -a/d
    git reflog master #查看本地log

    git checkout    git pull origin dev
    git remote -v #查看远程地址
    git remote add origin url #修改远程仓库
    git remote add upstream url #配置远程分支 git fetch upstream


    py
    pip install -r requirements.txt
    pipreqs ./ --encoding=utf8     pip freeze > requirements.txt

    pipenv install # 创建与本机相同python版本的虚拟环境
    pipenv shell # 激活虚拟环境
    exit # 退出虚拟环境
    pipenv --rm # 删除当前虚拟环境
    pipenv graph # 查看安装的所有包及依赖包
    pipenv uninstall --all # 卸载全部包
    pipenv graph # 查看安装的所有包及依赖包


```

# 数据库

```
	mysql
	group_concat(stageDoc)

	mongoose
	Category.find().where({parent:parent }).populate('categories partners.hero').limit(5).lean()
	Category.aggregate([聚合查询
            {$match:{parent:parent._id}},条件查询
            {表连接
              $lookup:{
                from:'articles',表 模型小写加s localField:'_id',主表键 foreignField:'categories',副表键 as:'newsList',拟名
                }
            },
		])

```

# java

```
    out.print() system.out.println()输出

    request.getRequestDispatcher("xxx.jsp?name").forward(request,response);
    window.location.href ="KindList.jsp?type="+encodeURIComponent(JSON.stringify(json.obj[i]));

```

# jquery

```
 		$(document).on("绑定事件"，["绑定属性"]，function(){});
    	$("对象:first") $("对象:even")偶数位 $("对象:odd")奇数位 $("对象:eq(index)")第几个 $("对象:empty")组内容为空
        $("对象").each(function(){遍历
            find()查找
            $("#kuan > div").empty()清空内容
        	setInterval(time,500)计时器
        })

```

# css

```css
	calc(100% - 5px) #计算
	letter-spacing：2px #字间隔 #字间距
	display: table-cell;
	vertical-align: middle; #垂直居中    position:absolute;top:50%;transform:translate(-50%,-50%);

	transform-style: preserve-3d;
	perspective：500px perspective-origin:top #3D透视
	transition: all 2s ease-in-out 0s #过渡

	animation: move 10s linear infinite;
	animation-direction: alternate; #反向
	@keyframes move{from{}to{}}  #关键帧
	#帧暂停
	animation-play-state:paused !important;
    -webkit-animation-play-state:paused !important;
    transform:
      translate|3d|Z (x,y) #移动
      scale(x,y) #缩|扩
      rotate|Z(deg) #旋转
      skew|X(deg|x,deg|y) #倾斜和旋转

	::before #伪类
    content:attr(data-before);
    span.attr('data-before',i.count)

  :after{
    border: 2px solid rgba(0, 38, 81, 0.75);
    border-top: 0;
    border-left: 0;
  }

	border: 4px dashed;
	background-repeat:no-repeat #不重复
   	background-size：auot | contain #大小
   	background-position：center center #位置

	overflow:hidden
	white-space:nowrap
	text-overflow:ellipsis #隐藏省略
	el-table :show-overflow-tooltip="true"

	overflow: hidden;
	display: -webkit-box;
	-webkit-box-orient: vertical;
	-webkit-line-clamp: 2;

  overflow-x:scroll
  white-space:nowrap #滚动条

  flex-wrap:nowrap
  flex-flow:wrap #自动换行
  align-items:center
  flex-direction: column
  justify-content: space-around | space-between | space-evenly | center | flex-end

  box-sizing: border-box; #向内部撑开
  cursor:pointer 手 text-decoration:line-through #横线
  text/border/box-shadow:5px 5px 5px purple,-5px -5px 5px green #阴影
  background:linear-gradient(rgb(0,0,0,0),rgb(0,0,0,1)) radial-gradient #渐变
  filter: drop-shadow(0rpx 2rpx 10rpx rgba(195, 206, 218, 0.31));
  clip-path: inset(0 round 60px)

  white-space:pre 保留空格和回车符
  @media screen and (max-width: 828px) {屏宽}

  element
	:header-cell-style :row-style  table

	clip-path: inset()   clip-path: inset(0 round 10px)
	position:absolute; clip:rect(50px 50px 200px 0px) #图形裁剪
  clip-path: polygon(0% 100%, 10% 0%, 90% 0%, 100% 100%) #四边形裁剪


	filter: grayscale(10%) #黑白

	background-image: linear-gradient(to bottom, #CBCBCB 0%, #CBCBCB 50%, transparent 50%); #可变虚线
	background-size: 2px 18px;
	background-repeat: repeat-y;
  background: `url(${require('../../static/images/1.png').default}) 0 0 / 100% 100%`,

	#标签
	<a type="checkbox" :checked="false"/> <a type="checkbox" checked="false"/>
	<span><svg style={it.svg} stroke-dasharray="3,3">
		<line x1="0" y1="100%" x2="100%" y2="0"  style={{stroke:'#73fdff',strokeWidth:'1'}}/>
	</svg> </span>
	&nbsp; &ensp;  &thinsp; &zwnj; &zwj;&emsp;

	{ required: true, message: '请输入用户名' },{pattern: /^[a-zA-Z][a-zA-Z0-9_]{5,16}$/,message: '用户名字母开头,长度5-16个字符(字母数字下划线)',},
	/^[1-9]\d{5}(18|19|20)\d{2}((0[1-9])|(1[0-2]))(([0-2][1-9])|10|20|30|31)\d{3}[0-9Xx]$/

```

# js

```js
    toFixed()
    shift  //删除数组中的第一个元素
    pop  //删除数组中的最后一个元素
    unshift //增加元素在数组的前面
    push //增加元素在数组的后面
    splice(start,n,addObj) //开始位置 删除个数，添加元素
		site.split("/").slice(0,site.split("/").length-1).join("/") //切割 新数组 拼接  split(/[T+.]/)

    flat //将多维数组转为一维数组
    arr.concat(arr)   [...arr,...arr] //合并数组  const { spots, picture, ...dataList } = e;
    arr.join('/')  //将数组转为字符串
    slice(start,end) //选中[start.end)之间的元素

    arr.find(i=>{})  //查找出(第一个)符合条件中的数组元素
    findIndex(fn(n,index),this)  //查找出第一个符合条件中的数组元素，所在的索引位置
    arr.indexOf('')  //查找值所在的位置
    arr.includes('arr[i]')  //查看数组中是否存在此元素

    reverse  //颠倒数组中的顺序
    sort((a,b)=>Math.random()-0.5)  //排序
    reduce((prev,cur,index,arr)=>{},0)  ((item.num/Two_1_data.reduce((a,b)=>{return b.num+a},0)*100)).toFixed(0)
		//数组对象去重
		let obj = {};
		let arr= res.data.reduce((cur,next)=>{
			console.log('cur',cur);
			console.log('next',next);
			obj[next.reportTime] ? "" : obj[next.reportTime] = true && cur.push(next);
			return cur;
		},[])

    every  //检测数组中元素是否都是符合条件 === bollean
    some  //检测数组中元素是否有满足条件的元素 === bollean
    arr.filter(i=>{}) //过滤，筛选出数组中的满足条件的保留，并且返回新的数组

    replace(/-/g, ".") //替换
		arr.splice(arr.map(n => n.value).indexOf("手机号码"), 1) //是否含有 删除
		if (!Array.isArray(arr)) {return}  arr=[...new Set(arr)]  //去重

		Cookie   //name value maxage path size Secure | 安全传输
		parseInt()   //保留整数
		Math.ceil()  //向上取整
		Math.ruond() //四舍五入
		Math.floor() //向下取整

		Math.random().toString(36) //转36进制
    toLowerCase() //转小写
		Math.sqrt() //开平方根
		substring(0,3)
		t= data.tickList.filter(e =>{
			return this.$route.query.goodsId === e.goodsId
		})

    Array.from(Array(12), () => 0)
    requestAnimationFrame //定时器

    console.log = () => {}
    list.unshift(list.splice(index , 1)[0]); //置顶

		{value,value} = await new Promise((resolve,reject)=>resolve({value}))
		await Promise.all(list:[new Promise()]);

		setAttribute('','') removeAttribute() getAttribute() /*事件*/
		mouseenter()mouseleave()   mouseover()mouseout()
		@scroll


		// 正则
		split(/[' ']/g) //所有空格
		split(/[a-z]+/g) //所有连续的字母
		const AnyArr: {[key: string]: any} []=[]

    const { id: outherName, name:{childData},...outher } = n; //解构

```

# python

```python
   sorted(pics, key=lambda x:int(x)) #排序

```

# react

```
	className={`${this.state.ifjiu?'noshow':'show'}
	${(i.lineNo!='')?'dui':''}`}

	render() { Portals
    	return ReactDOM.createPortal(this.props.children,this.el,);
  	}

  	<StrictMode /> 严格模式|包含组件

  	display:"block", height:'80px',


```

# vue

```
	scrollBehavior(to,from,savedPosition){浏览记录,第一次不缓存且仅缓存第一次} beforeEach() beforeEnter() beforeRouteLeave()
	<keepAlive :include='' :exclude='' :max >   activated()  deactivated()
	beforeCreate() created()
	beforeMount() mounted()
	beforeUpdate() updated()
	beforeDestroy() destroyed()
	this.$nextTick(()=>{在下次DOM更新循环结束之后执行延迟回调。在这里面的代码会等到dom更新以后再执行。})
	props{ type:''  defaule:''  require:true}
	methods{} components{组件} computed{计算} mixins{代码块} filter(name,fn) filters{过滤器}
	watch{ 'obj.name' | obj:{ handler(newArr){},immediate:true,deep: true}  }
	directives{指令
        'name':{	bind,inserted,update,componentUpdated,unbind
            inserted/bind(el,vnode){
				node.modifiers 修饰符
				node.expression 表达式
            }
        }
	}

	父子组件通信
	props  this.$emit('fnName',obj)
	this.$refs.name.fun()=this.$parent/children.fun()  获取组件实例
	this.$attrs
	envetBus
	v-bind='$attrs' v-on='$listeners'  inheritAttrs: false,   会触发组件渲染
	provide(){return {datas: this.datas}  } inject:['datas']  异步无数据


	<Child v-model=''></Child>
	<Child @hook:mounted="doSomething" ></Child>  监听子组件mounted事件执行时doSomething
	v-on="{change:hh,click:aa}" 绑定多事件

	object.assign(this.$data, this.$options.data())
	this.$set(this.$data,'modelItem',this.$options.data().modelItem) 初始化data


	@close 监听弹窗
	event.cancelBubble = true  @click.stop   事件冒泡

	:style="{'backgroundColor':(form.status=='已通过'?'#1ea310':'#edf0f0')}"
	:style="{backgroundImage:'url('+require('@/assets/images/'+n.GoodImg.replace('/img/',''))+')'}"

	row.powers.search(eval("("+sessionStorage.getItem("user")+")").userName)!="-1"

	<slot name='arr' :value="sol"></slot>
	<template #default="sol"> <template #name="sol">


	@click='$router.push({name:"menu",params:{id:n.deskId,name:n.deskCaption}})'  this.$route.params

	# 文件位置:/fabric-deploy/bootstrap/config/configOrg.yaml


	3
	const  emits = defineEmits(['ensure','update:edit']);
	emits('update:edit',true);

  vue - css
  :style="{ '--color': props.color }"
  border-left: 2px solid var(--color);

```

# other

```
	this.model=Object.assign({},this.model,res.data)  slice(0)  {...arr} 浅拷贝
	JSON.parse(JSON.stringify(state));  深拷贝
	$set(model,'icon',res.url)

	Object.prototype.toString.call() , constructor , arr instanceof Array  typeof isPrototypeOf  Object.getPrototypeOf()  类型判断

	const scale = (n,inMin,inMax,outerMin,outerMax) => (n - inMin) * (outerMax - outerMin) / (inMax - inMin) + outerMin;

  #git
  git remote -v   git remote set-url origin #更换库
  git pull origin test --allow-unrelated-histories 异库合并
  git reset --hard origin/master

  git@github.com:nieqiaoxi/web.git
```

# resource

```js

```

# MY

```
	wn.run/
	you-get -o C:\Users\Qianlu\Videos\you-get
	copy 1.jpg/b+ 2.rar =3.jpg

	明 YN/uMDTGOHafYJ1q	YRCBOTyVOXDYX4OG
  @ZLQTkSs/hli#3RuwOv2IJ6EXrHgfP0UF!&ZQGYtMy8*A~5x1zB%W7cm^D4qp$LjoNKC

```
